"""Day 16: ablation table comparing retrieval strategies
(dense only vs. hybrid vs. hybrid + rerank) against the golden set."""


def run_ablation(golden_set: list[dict]) -> dict:
    # TODO(day 16): run each retrieval strategy over the golden set,
    # report recall/precision/latency per strategy.
    raise NotImplementedError("run_ablation: not implemented yet (day 16)")
