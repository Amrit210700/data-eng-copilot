"""Day 14: index the schema/metadata catalog into Chroma."""


def index_catalog(catalog_dir: str, persist_dir: str) -> None:
    # TODO(day 14): load catalog docs (tables, columns, descriptions),
    # chunk, embed, and upsert into a Chroma collection.
    raise NotImplementedError("index_catalog: not implemented yet (day 14)")
